21. A subarray is any array consisting of consecutive elements of 'a'. E.g. if a = [a1, a2, a3, a4], then [a2, a3, a4] is a
subarray of length 3 but [a1, a3, a4] is not a subarray.
22. Find the largest sum from a contiguous subarray. Solution: Set variables called 'max_sum' and 'current_sum'.
Iterate once through the array. Set max_sum to current_sum if current_sum >= max_sum. Reset current_sum to
0 if current_sum becomes negative.
23. Find the number of occurrences a 3-letter string can be found in a larger one (e.g., SHL in SSQHUL is found
twice).
24. Given an array of integers, find the subarray with the largest sum and return its sum – maximum subarray
question.
25. Given a list and an integer, check if the integer can be written as the sum of elements in the list.
26. Given an m x n binary matrix filled with 0’s and 1’s, find the largest square containing only 1’s and return its area –
maximal square question.

27. There are two strings such as a=abc#abc#’ and b=’ababb#’and the ‘#’ denotes a backspace in the keyboard
(delete the former character). Check if the true a and b is the same.
28. Given a list such as. [3, 2, 1, 4, 6, 5], get the maximum n such that after split the list into n parts and sort each
part, the final list would be a non-descending list. The answer is 2 since you can divide it to [3, 2, 1] and [4, 6, 5],
then sort them and get [1, 2, 3] and [4, 5, 6], combine them and you get a non-descending list.
29. Given a list of movie durations, you have to find out the minimum day of watching them. You can combine
movies, but each day you cannot watch more than 3 hours.
30. A file uploading background. Given what positions are occupied, you have to figure out how to fill the rest of
positions by 2^n size of files.
31. Whether you know about the merge sort and quick sort. What is the corresponding time complexity?
32. What is object oriented programming? Give an example of a project you’ve used OOP in before.
33. Give the longest subarray which sums to zero.
34. Given a sphere in 3-D with points (x,y,z) randomly distributed on its surface, what is the expectation E[x] and
variance Var[x] of coordinate x ?
a. Solution: by symmetry of distribution, E[x] = coordinate x of the center of the sphere. For the variance,
one might notice that the variables x,y,z are equally distributed, therefore one can compute E[x^2] using
the equation of a sphere ( E[r^2] = E[x^2]+E[y^2]+E[z^2] = 3E[x^2]) and use Var[x] = E[x^2]-E[x]^2 to
compute the variance.

35. You are given an array "arr" of int that you have to traverse by consecutive jumps. Given that you're at index i,
you can jump to any other index within [i+1, i+arr[i]]. Write an algorithm to find the minimum number of jump
require to traverse the array.


class Solution(object):
    def minSwapsCouples(self, row):
        N = len(row) / 2
        pairs = [[row[i]/2, row[i+1]/2]
                 for i in xrange(0, 2*N, 2)
                 if row[i]/2 != row[i+1]/2]

        def swap(a, b, c, d):
            pairs[a][b], pairs[c][d] = pairs[c][d], pairs[a][b]

        def solve(i = 0):
            if i == len(pairs): return 0
            x, y = pairs[i]
            if x == y: return solve(i+1)

            for j in xrange(i+1, len(pairs)):
                for k in xrange(2):
                    if pairs[j][k] == x: jx = j, k
                    if pairs[j][k] == y: jy = j, k

            swap(i, 1, *jx)
            ans1 = 1 + solve(i+1)
            swap(i, 1, *jx)

            swap(i, 0, *jy)
            ans2 = 1 + solve(i+1)
            swap(i, 0, *jy)

            return min(ans1, ans2)

        return solve()


class Solution(object):
    def minSwapsCouples(self, row):
        ans = 0
        for i in xrange(0, len(row), 2):
            x = row[i]
            if row[i+1] == x^1: continue
            ans += 1
            for j in xrange(i+1, len(row)):
                if row[j] == x^1:
                    row[i+1], row[j] = row[j], row[i+1]
                    break
        return ansa. Solution: use dynamic programming approach by initializing an array dp and fill it using the recurrence
relation : dp[i] = min(dp[i],dp[i+j]+1) with j within 1,...,arr[i].
